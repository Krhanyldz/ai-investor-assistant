import Link from "next/link";
import type { PortfolioPosition } from "@/domain/portfolio";
import type { WatchlistItem } from "@/domain/watchlist";
function number(value:number){return new Intl.NumberFormat("en-US",{maximumFractionDigits:2}).format(value)}
export function DashboardOverview({positions,watchlist,portfolioError,watchlistError}:{positions:PortfolioPosition[];watchlist:WatchlistItem[];portfolioError?:string;watchlistError?:string}){
 const totals=positions.reduce<Record<string,number>>((all,item)=>({...all,[item.currency]:(all[item.currency]??0)+item.quantity*item.averageCost}),{});
 return <div className="space-y-6"><div className="grid gap-4 md:grid-cols-3"><section className="rounded-lg border border-zinc-800 bg-zinc-900/80 p-5"><p className="text-sm text-zinc-400">Portfolio positions</p><p className="mt-2 text-3xl font-semibold">{positions.length}</p><Link href="/portfolio" className="mt-3 inline-block text-sm text-emerald-300 underline">Manage portfolio</Link></section><section className="rounded-lg border border-zinc-800 bg-zinc-900/80 p-5"><p className="text-sm text-zinc-400">Watchlist items</p><p className="mt-2 text-3xl font-semibold">{watchlist.length}</p><Link href="/watchlist" className="mt-3 inline-block text-sm text-emerald-300 underline">Open watchlist</Link></section><section className="rounded-lg border border-zinc-800 bg-zinc-900/80 p-5"><p className="text-sm text-zinc-400">Entered cost basis</p>{Object.keys(totals).length?Object.entries(totals).map(([currency,total])=><p key={currency} className="mt-2 text-xl font-semibold">{number(total)} {currency}</p>):<p className="mt-2 text-sm text-zinc-500">No positions entered</p>}<p className="mt-2 text-xs text-zinc-500">Not live market value</p></section></div>{portfolioError||watchlistError?<p role="alert" className="rounded-lg border border-amber-400/30 bg-amber-950/30 p-4 text-amber-100">Some dashboard data could not be loaded. Try again later.</p>:null}<div className="grid gap-6 lg:grid-cols-2"><section className="rounded-lg border border-zinc-800 bg-zinc-900/80 p-5"><h2 className="font-semibold">Recent watchlist research</h2>{watchlist.length?<ul className="mt-4 space-y-3">{watchlist.slice(0,5).map(item=><li key={item.id}><Link href={`/stocks/${encodeURIComponent(item.symbol)}`} className="text-emerald-300 underline">{item.symbol}</Link></li>)}</ul>:<p className="mt-4 text-sm text-zinc-400">Your watchlist is empty.</p>}</section><section className="rounded-lg border border-zinc-800 bg-zinc-900/80 p-5"><h2 className="font-semibold">Start research</h2><div className="mt-4 flex flex-wrap gap-3"><Link href="/stocks" className="rounded-md border border-zinc-700 px-4 py-2 text-sm">Search stocks</Link><Link href="/etfs" className="rounded-md border border-zinc-700 px-4 py-2 text-sm">Search ETFs</Link><Link href="/ai-research" className="rounded-md border border-zinc-700 px-4 py-2 text-sm">AI research</Link></div></section></div></div>
}
